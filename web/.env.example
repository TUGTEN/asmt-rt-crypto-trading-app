# Backend base URL used by the browser (no trailing slash needed).
# Local dev value — copy to .env.local. Production default lives in
# lib/config.ts (https://pf-api.0kv.in); on Vercel set NEXT_PUBLIC_API_URL
# in the dashboard (needs a redeploy to take effect).
NEXT_PUBLIC_API_URL=http://localhost:8080
